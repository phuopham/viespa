# MySQL connection properties
spring.datasource.url=jdbc:mysql://localhost:3306/viespa
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

# Log JPA queries
# Comment this in production
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Drop and create new tables (create, create-drop, validate, update)
# Only for testing purposes - comment this in production
spring.jpa.hibernate.ddl-auto=create-drop

# Show error message for testing API
server.error.include-message=always

# Hibernate SQL dialect
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# Email for registration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.userName=testpskg
spring.mail.password=eghxucwqopjoguro

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#Set up for logger
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG
logging.level.org.springframework.jdbc.core.StatementCreatorUtils=TRACE
